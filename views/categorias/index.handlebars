<h1>Categorias: </h1>
<br>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h2>{{nome}}</h2></a>
        </div>
    </div>
{{else}}
    <h2 class="mt-4">Nenhuma categoria registrada.</h2>
{{/each}}
